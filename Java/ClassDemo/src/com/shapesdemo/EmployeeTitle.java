package com.shapesdemo;

public enum EmployeeTitle { // all are public static final by default
                            // just like interface variables
                            // so they can be used in interfaces
    CEO,
    CHAIRMAN,
    DIRECTOR,
    MANAGER,
    SUPERVISOR
}
