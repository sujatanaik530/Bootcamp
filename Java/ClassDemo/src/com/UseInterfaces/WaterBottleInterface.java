package com.UseInterfaces;

public interface WaterBottleInterface {
    // Interface variables are final. Their values cannot be changed.
    String color = "Pink";

    // Interface methods are abstract. They cannot have an implementation here.
    void fillBottle();
    void emptyBottle();
}
